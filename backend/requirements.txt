# --- Agent server (FastAPI) ---
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# --- Clinical-protocol-review agents (real LLM mode) ---
# Required only when OPENAI_API_KEY is set (otherwise the server runs in mock mode).
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
openai>=1.0.0
pypdf
tiktoken

# --- Shared vector memory (RAG across tools) ---
faiss-cpu>=1.7.4
numpy>=1.25
